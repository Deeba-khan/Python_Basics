#Exponent and Assign - "**="
#ExponentAssign operator used to calculate exponent(raise the power) value using operands, then assign the final result to left side operand.

a = 5
b = 2
a **= b #[a = a ** b]
print("Calculate the exponent of the values by using ExponentAssign '**=' operator: ", a)