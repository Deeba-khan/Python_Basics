#Add and Assign - "+="
#AddAssign operator used to add right side operand with left side operand, then assign the final result to left side operand.

a = 2 
b = 3
a += b #[a = a + b]
print("Add the values by using AddAssign '+=' operator: ", a)